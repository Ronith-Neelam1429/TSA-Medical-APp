import 'package:flutter/material.dart';

class CameraDescription1 extends StatelessWidget {
  const CameraDescription1({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Camera",
      style: TextStyle(fontSize: 15, fontWeight: FontWeight.w500),
    );
  }
}
